<!-- Code adapted and modified from https://github.com/osu-cs340-ecampus/nodejs-starter-app/tree/main -->

<!-- index.hbs -->
<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>


{{!-- Create a table --}}
<table id="copies-table">>

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th>delete</th>
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr data-value={{this.copyID}}>>
            <td>{{this.copyID}}</td>
            <td>{{this.mediaID}}</td>
            <td>{{this.mediaTitle}}</td>
            <td>{{this.creatorName}}</td>
            <td>{{this.mediaType}}</td>
            <td>{{this.customerID}}</td>
            <td>{{this.customerFName}}</td>
            <td>{{this.customerLName}}</td>
            <td><button onclick="deleteCopy({{this.copyID}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>


<script src="./js/delete_copy.js"></script>

<!-- /views/index.hbs -->
<h2>Adding Data using an HTML form</h2>
<p>To add a new copy, please enter the information below and click `Submit`</p>
<form id="add-copy-form" method="POST" action="/add-copy-form">
    <label for="input-mediaID">Media ID: </label>
    <input type="text" name="input-mediaID" id="input-mediaID-html">
    
    <label for="input-customerID">Customer ID: </label>
    <input type="text" name="input-customerID" id="input-customerID-html">

    <!-- <label for="input-homeworld">Homeworld: </label>
    <select name="input-homeworld" id="input-homeworld-ajax">
        <option value="">Select a Planet</option>
        {{#each planets}}
        <option value="{{this.id}}">{{this.name}}</option>
        {{/each}}
    </select>

    <label for="input-age">Age: </label>
    <input type="number" name="input-age" id="input-age-ajax"> -->

    <input type="submit">
</form>


<!-- /views/index.hbs -->

{{!-- Form to add a record to the table  --}}
<h2>Adding Data with AJAX</h2>
<p>To add a new copy, please enter their information below and click 'Submit'!</p>
<form id="add-copy-form-ajax">
    <label for="input-mediaID">Media ID: </label>
    <input type="text" name="input-mediaID" id="input-mediaID-ajax">
    
    <label for="input-customerID">Customer ID: </label>
    <input type="text" name="input-customerID" id="input-customerID-ajax">


    <!-- <label for="input-homeworld">Homeworld: </label>
    <select name="input-homeworld" id="input-homeworld">
        <option value="">Select a Planet</option>
        {{#each planets}}
        <option value="{{this.id}}">{{this.name}}</option>
        {{/each}}
    </select>

    <label for="input-age">Age: </label>
    <input type="number" name="input-age" id="input-age"> -->

    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src="./js/add_copy.js"></script>


<h2>Search Using an HTML Form</h2>
<p>Search by media ID using the field below.</p>
<form id="search-copy-html" method='GET' action='/'>
    <label for="input-mediaID">Search by Media ID: </label>
    <input type="text" name="mediaID" id="input-mediaID-html-search">
    
    <input type="submit">
    <a href='/'><button>Reset</button></a>
</form>

<h2>Updating A User's Media ID</h2>
<p>To update a copy's media ID, select their customer ID from the drop-down menu on the left and their desired media ID from the drop down menu on the right</p>
<form id="update-copy-form-ajax">

    <label for="input-customerID">Customer ID: </label>
    <select name="input-customerID" id="mySelect">
        <option value="test">Select a CustomerID</option>
        {{#each customerID}}
        <option value="{{this.copyID}}">{{this.customerID}}}</option>
        <!-- <option value="{{this.id}}">{{this.fname}} {{this.lname}}</option> -->
        {{/each}}
    </select>

    <label for="input-mediaID">Media ID: </label>
    <select name="input-mediaID" id="input-mediaID-update">
        <option>Select a Media ID</option>
        {{#each mediaID}}
        <option value="{{this.copyID}}">{{this.mediaID}}</option>
        {{/each}}
    </select>
    <input type="submit">

</form>

<script src="./js/update_copy.js"></script>